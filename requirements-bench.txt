# ベンチ（#9）・モデル取得用の追加依存。
# エンジン系依存（faster-whisper / llama-cpp-python / transformers）は
# requirements.txt（ランタイム）側に移動済み（#11/#12）。
-r requirements.txt
huggingface_hub>=0.23
psutil>=5.9
websockets>=12  # 性能受け入れ試験の負荷クライアント（#17。uvicorn[standard]経由でも入る）
