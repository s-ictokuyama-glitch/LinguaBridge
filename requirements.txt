# ランタイム依存
# llama-cpp-python はCPU用プリビルトwheelのインデックスを使う
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
PyYAML>=6.0
numpy>=1.26
faster-whisper>=1.2       # ASR切替候補（#11）。Silero VAD (ONNX) と ctranslate2 も同梱/依存で入る
sherpa-onnx>=1.13         # ASR既定（#28 ReazonSpeech K2 v2）。Apache-2.0・wheel 19MB・ビルド不要
llama-cpp-python>=0.2.85  # hy-mt2 翻訳エンジン（#12）
transformers>=4.40        # NLLBトークナイザ（torch不要）
sentencepiece>=0.2
cryptography>=42          # 自己署名TLS証明書の生成（#16 scripts/make_cert.py）
psutil>=5.9               # NIC稼働状態のローカル取得（#38）
